VITE_SUPABASE_PROJECT_ID="lgcxyxyidoirqmbdlldh"
VITE_SUPABASE_PUBLISHABLE_KEY="<anon-key>"
# A RAIZ do projeto, sem sufixo de API. O painel do Supabase mostra "https://<projeto>
# .supabase.co/rest/v1" na página das chaves; colar isso aqui (foi o que estava na Vercel
# até 25/08/26) manda as chamadas de Edge Function para o PostgREST, que responde 401 e
# derruba o Assistente calado. O código hoje tira o endereço do próprio cliente
# (src/lib/urlFuncao.ts), então isto é só reserva — mas mantenha certo.
VITE_SUPABASE_URL="https://lgcxyxyidoirqmbdlldh.supabase.co"
VITE_OMIE_WEBHOOK_URL=https://n8n.codless.cloud/webhook/enviar-anexo-omie

# Domínio publicado do Hub — vai DENTRO dos links compartilhados de anotação
# (/notas/<id> e /n/<token>). Sem isto, um link copiado de dentro do preview do
# Lovable apontaria para o preview e não abriria para quem está de fora.
# Mesmo valor de hub_base_url() no banco. Em localhost é ignorado.
VITE_HUB_URL="https://findashtakeat.lovable.app"
