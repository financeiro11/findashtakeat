# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code (config local da ferramenta — não faz parte do app)
.claude/

# Estado local do Supabase CLI (ref do projeto, cache) — não versionar.
supabase/.temp/

# Segredo local — token de ferramenta. NUNCA versionar.
.ltok

# Rascunhos de depuração (Playwright apontando para o dev server, arquivos soltos).
simple-test.js
test-cartao.mjs
x

# Relatórios gerados para conferência (planilhas e CSV de apuração). São DADO,
# não código: trazem nome de cliente, CNPJ e valor, e nascem de uma consulta que
# se pode repetir a qualquer hora. Versionar congelaria o retrato de um dia
# dentro do repositório, e o repositório não é onde esse retrato deve morar.
saida/
